"""Spintax agent tooling — synonym + syntax axis tools.

These modules expose deterministic helpers that the spintax runner exposes
as agent tools. They live behind the `/api/spintax` HTTP surface; the
runner registers schemas in `app/spintax_runner.py`.

Source: pi-agents/tools/spintax_agent_tools/ (April 2026 prototype).
"""
